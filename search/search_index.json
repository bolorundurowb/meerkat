{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Meerkat","text":"<p>Meerkat is an Object Document Mapper (ODM) for MongoDB in .NET. It wraps the official MongoDB driver and simplifies common data access patterns, modelling, querying, persistence, and indexing with a clean, strongly-typed API.</p>"},{"location":"#installation","title":"Installation","text":".NET CLIPackage Manager Console.csproj PackageReference <pre><code>dotnet add package meerkat\n</code></pre> <pre><code>Install-Package meerkat\n</code></pre> <pre><code>&lt;PackageReference Include=\"meerkat\" Version=\"3.0.0\" /&gt;\n</code></pre>"},{"location":"#quick-setup","title":"Quick Setup","text":"<p>Call <code>Connect</code> once at application startup, before executing any Meerkat operations:</p> <pre><code>using meerkat;\n\nMeerkat.Connect(\"mongodb://user:password@host:port/database-name\");\n</code></pre>"},{"location":"#verifying-indexes-at-startup","title":"Verifying Indexes at Startup","text":"<p>To create and verify attribute-declared indexes at startup, call <code>EnsureIndexesAsync</code> right after <code>Connect</code>:</p> <pre><code>await Meerkat.EnsureIndexesAsync(typeof(MyApp).Assembly);\n</code></pre> <p>This scans the assembly for concrete <code>Schema&lt;TId&gt;</code> types, creates any indexes declared via index attributes idempotently, and throws <code>IndexVerificationException</code> if an expected index is missing.</p>"},{"location":"#quick-example","title":"Quick Example","text":"<p>Define a model inheriting from <code>Schema&lt;TId&gt;</code>:</p> <pre><code>using meerkat;\nusing meerkat.Attributes;\nusing MongoDB.Bson;\n\n[Collection(TrackTimestamps = true)]\npublic class Student : Schema&lt;ObjectId&gt;\n{\n    public string FirstName { get; set; } = string.Empty;\n    public string LastName { get; set; } = string.Empty;\n    public int Age { get; set; }\n\n    public Student()\n    {\n        Id = ObjectId.GenerateNewId();\n    }\n}\n</code></pre> <p>Save and query documents:</p> <pre><code>// Persist or update\nvar student = new Student { FirstName = \"Ada\", LastName = \"Lovelace\", Age = 36 };\nawait student.SaveAsync();\n\n// Query by ID\nvar result = await Meerkat.FindByIdAsync&lt;Student, ObjectId&gt;(student.Id);\n\n// Query with LINQ\nvar students = await Meerkat.Query&lt;Student, ObjectId&gt;()\n    .Where(s =&gt; s.LastName == \"Lovelace\")\n    .ToListAsync();\n</code></pre>"},{"location":"#next-steps","title":"Next Steps","text":"<p>Explore the detailed documentation topics:</p> <ul> <li>Defining Models \u2014 Base schema, identifiers, collection configuration, timestamps, and soft delete.</li> <li>Querying \u2014 Find by ID, predicates, LINQ queries, counts, and existence checks.</li> <li>Persistence &amp; Transactions \u2014 Document upserts, bulk persistence, deletions, and ambient multi-document transactions.</li> <li>Atomic Updates \u2014 Fluent update builder and atomic numeric increment/decrement helpers.</li> <li>Indexing \u2014 Single-field, unique, compound, TTL, geospatial indexes, and startup verification.</li> <li>Transforms &amp; Hooks \u2014 String casing transforms and <code>PreSave</code>/<code>PostSave</code> lifecycle hooks.</li> <li>Architecture \u2014 Deep dive into library design, runtime model, and request flows.</li> <li>Breaking Changes \u2014 Upgrade notes and breaking change history across major versions.</li> </ul>"},{"location":"architecture/","title":"Architecture","text":"<p>This document outlines the internal architecture, design principles, runtime model, and request flows of Meerkat.</p>"},{"location":"architecture/#1-purpose-design-goals","title":"1. Purpose &amp; Design Goals","text":"<p>Meerkat is designed as a thin, strongly-typed Object Document Mapper (ODM) on top of the official <code>MongoDB.Driver</code> for .NET (<code>netstandard2.0</code>).</p> <ul> <li>Assembly: <code>Meerkat</code></li> <li>Package: <code>meerkat</code></li> <li>Underlying Driver: <code>MongoDB.Driver</code> (3.x)</li> </ul> <p>Its primary goal is to eliminate boilerplate data-access code and provide intuitive ActiveRecord-style and static querying ergonomics while retaining direct escape hatches to the raw MongoDB driver whenever advanced functionality is required.</p>"},{"location":"architecture/#2-repository-layout","title":"2. Repository Layout","text":"<p>The repository is organised into the core library and a comprehensive test suite:</p> <ul> <li><code>src/meerkat</code>: The .NET Standard 2.0 library containing the ODM logic.</li> <li><code>src/meerkat.Tests</code>: The unit and integration test suite targeting modern .NET.</li> </ul>"},{"location":"architecture/#feature-split-partials","title":"Feature-Split Partials","text":"<p>The core static class <code>Meerkat</code> is organised across focused partial classes grouped by responsibility:</p> File Responsibility <code>Meerkat.cs</code> Connection initialisation (<code>Connect</code>), lazy database/client access, session storage, and core collection retrieval. <code>Meerkat.Collections.cs</code> Resolution of <code>IMongoCollection&lt;TSchema&gt;</code> from types and trigger for index creation. <code>Meerkat.Queries.cs</code> Implementation of <code>FindById</code>, <code>FindOne</code>, and <code>Find</code> (sync and async). <code>Meerkat.Removals.cs</code> Implementation of <code>RemoveById</code>, <code>RemoveOne</code>, <code>Remove</code>, <code>HardRemove*</code>, and <code>Restore*</code>. <code>Meerkat.Counting.cs</code> Implementation of <code>Count</code> and <code>CountAsync</code> overloads. <code>Meerkat.Updates.cs</code> Atomic numeric increment and decrement operations (<code>IncrementById</code>, <code>DecrementByFilter</code>, etc.). <code>Meerkat.PartialUpdates.cs</code> Entry points for fluent partial updates (<code>Update</code>, <code>UpdateOne</code>, <code>UpdateMany</code>, <code>UpdateByFilter</code>). <code>Meerkat.Indexing.cs</code> Index reflection, creation, and startup verification (<code>EnsureIndexes</code>). <code>Meerkat.SoftDelete.cs</code> Soft-delete filter creation, update definitions, and helper queries. <code>Meerkat.Transactions.cs</code> Ambient session management and transaction execution (<code>WithTransaction</code>). <p>Other core files include:</p> <ul> <li><code>Schema.cs</code>: The generic base class <code>Schema&lt;TId&gt;</code> implementing <code>Save()</code>, <code>SaveAsync()</code>, <code>Delete()</code>, timestamps, and lifecycle hooks.</li> <li><code>Updates/UpdateBuilder.cs</code>: Fluent builder for constructing MongoDB update definitions.</li> <li><code>Collections/Enumerables.cs</code>: <code>SaveAll</code> and <code>SaveAllAsync</code> extension methods on <code>IEnumerable&lt;TSchema&gt;</code>.</li> <li><code>Attributes/*</code>: Attributes for configuration (<code>[Collection]</code>), indexing (<code>[SingleFieldIndex]</code>, <code>[UniqueIndex]</code>, <code>[CompoundIndex]</code>, <code>[GeospatialIndex]</code>), and casing (<code>[Lowercase]</code>, <code>[Uppercase]</code>).</li> </ul>"},{"location":"architecture/#3-public-api-surface","title":"3. Public API Surface","text":"<p>Meerkat exposes its capabilities through three primary paradigms:</p> <ol> <li>Static <code>Meerkat</code> API: Static generic methods taking <code>&lt;TSchema, TId&gt;</code> for data querying, counting, existence checks, removals, updates, and transactions.</li> <li>Generic <code>Schema&lt;TId&gt;</code> Base Class: Instance methods on models (<code>Save</code>, <code>SaveAsync</code>, <code>Delete</code>, <code>DeleteAsync</code>) and overridable lifecycle hooks (<code>PreSave</code>, <code>PostSave</code>).</li> <li>Declarative Attributes: Class- and property-level metadata controlling collection names, indexing rules, TTLs, and string transformations.</li> <li>Extension Methods: <code>SaveAll</code> and <code>SaveAllAsync</code> on <code>IEnumerable&lt;TSchema&gt;</code> in the <code>meerkat.Collections</code> namespace.</li> <li>Escape Hatch: <code>Meerkat.Collection&lt;TSchema, TId&gt;()</code>, <code>Meerkat.Database</code>, and <code>Meerkat.Client</code> provide direct, unrestricted access to native MongoDB driver types (<code>IMongoCollection&lt;TSchema&gt;</code>, <code>IMongoDatabase</code>, <code>IMongoClient</code>).</li> </ol>"},{"location":"architecture/#4-runtime-model","title":"4. Runtime Model","text":"<pre><code>flowchart TD\n  App[App startup] --&gt; Connect[Meerkat.Connect]\n  Connect --&gt; LazyDB[Lazy IMongoDatabase]\n  Op[Query Save Update] --&gt; GCF[GetCollectionForType]\n  GCF --&gt; LazyDB\n  GCF --&gt; Name[Collection name]\n  GCF --&gt; Idx[Indexes once per type]\n  GCF --&gt; Coll[IMongoCollection]\n  Save[Schema.Save] --&gt; Hooks[Timestamps transforms PreSave]\n  Hooks --&gt; Upsert[ReplaceOne upsert]\n  Upsert --&gt; Post[PostSave]\n  Part[UpdateBuilder] --&gt; Coll</code></pre>"},{"location":"architecture/#connection-initialisation","title":"Connection Initialisation","text":"<p>Calling <code>Meerkat.Connect(connectionString)</code>: 1. Parses the connection string using <code>ciu-parser</code> (<code>CredentialsParser.Parse</code>) to split the URL and target database name. 2. Instantiates <code>Lazy&lt;IMongoClient&gt;</code> and <code>Lazy&lt;IMongoDatabase&gt;</code>. Connection creation and database binding are deferred until the first operation executes.</p>"},{"location":"architecture/#collection-resolution-caching","title":"Collection Resolution &amp; Caching","text":"<p>Every operation routes through <code>GetCollectionForType&lt;TSchema, TId&gt;()</code>: 1. Name Resolution: Inspects <code>[Collection(Name = \"...\")]</code> on the schema type. If omitted, derives the name using <code>PluralisationService</code> (lowercased and pluralised, e.g., <code>BlogPost</code> \u2192 <code>blogposts</code>). 2. Index Initialisation (Once per Type): Checks <code>ConcurrentDictionary&lt;string, bool&gt; SchemasWithCheckedIndices</code>. If the schema type has not yet been processed in the current process lifetime, Meerkat inspects index attributes on the type, builds the corresponding MongoDB index models, and creates them asynchronously on the collection. 3. Ambient Session Binding: If an active ambient transaction exists in <code>AsyncLocal&lt;IClientSessionHandle?&gt; CurrentSession</code>, operations automatically join the session.</p>"},{"location":"architecture/#5-write-paths-replace-upsert-vs-operator-updates","title":"5. Write Paths: Replace Upsert vs. Operator Updates","text":"<p>Meerkat separates write paths into two distinct categories depending on whether the full entity is being persisted or specific fields are being modified atomically:</p>"},{"location":"architecture/#full-document-upsert-save-saveall","title":"Full Document Upsert (<code>Save</code> / <code>SaveAll</code>)","text":"<ul> <li>Mechanism: Issues a MongoDB <code>ReplaceOne</code> / <code>BulkWrite</code> with <code>IsUpsert = true</code>, matching by document <code>Id</code>.</li> <li>Timestamps: Sets <code>CreatedAt</code> on first persistence and <code>UpdatedAt</code> on every save if <code>TrackTimestamps = true</code>.</li> <li>Transformations: Executes string transformations (<code>[Lowercase]</code>, <code>[Uppercase]</code>).</li> <li>Lifecycle Hooks: Invokes <code>PreSave()</code> before the write and <code>PostSave()</code> after completion.</li> </ul>"},{"location":"architecture/#atomic-field-updates-updatebuilder-inc-helpers","title":"Atomic Field Updates (<code>UpdateBuilder</code> &amp; <code>$inc</code> Helpers)","text":"<ul> <li>Mechanism: Issues MongoDB update operators (<code>$set</code>, <code>$unset</code>, <code>$push</code>, <code>$pull</code>, <code>$addToSet</code>, <code>$inc</code>) directly using <code>UpdateOne</code>, <code>UpdateMany</code>, or <code>FindOneAndUpdate</code>.</li> <li>Timestamps: Automatically sets <code>UpdatedAt</code> to UTC now if <code>TrackTimestamps = true</code>.</li> <li>Bypassed Features: Bypasses <code>CreatedAt</code>, string case transformations, and <code>PreSave()</code> / <code>PostSave()</code> lifecycle hooks since whole documents are neither deserialised nor fully rewritten.</li> </ul>"},{"location":"architecture/#6-soft-delete","title":"6. Soft Delete","text":"<p>When <code>[Collection(SoftDelete = true)]</code> is set:</p> <ul> <li><code>Schema&lt;TId&gt;</code> exposes <code>DeletedAt</code> (<code>DateTimeOffset?</code>) and computed <code>IsDeleted</code>.</li> <li>Default query, count, existence, and update paths exclude documents where <code>DeletedAt</code> is set.</li> <li><code>Remove*</code> and instance <code>Delete</code> set <code>DeletedAt</code> instead of deleting the document.</li> <li><code>HardRemove*</code> / <code>HardDelete</code> permanently remove documents; <code>Restore*</code> clears <code>DeletedAt</code>.</li> <li>Callers can opt into including deleted documents via <code>includeDeleted: true</code> on query APIs and <code>Query</code>.</li> </ul>"},{"location":"architecture/#7-ambient-transactions","title":"7. Ambient Transactions","text":"<p><code>Meerkat.WithTransaction</code> / <code>WithTransactionAsync</code> open a client session, start a transaction, and store the session in <code>AsyncLocal&lt;IClientSessionHandle?&gt;</code>. Nested Meerkat operations (saves, queries, updates, removals) read that ambient session and participate automatically \u2014 callers do not pass session handles through each API. On success the transaction commits; on failure it aborts and the ambient session is cleared.</p>"},{"location":"architecture/#8-index-lifecycle","title":"8. Index Lifecycle","text":"<p>Indexes declared via attributes are created in two ways:</p> <ol> <li>Lazy (default): On first <code>GetCollectionForType</code> for a CLR type, Meerkat reflects index attributes and issues <code>CreateMany</code> / <code>CreateOne</code> once per process (tracked in <code>SchemasWithCheckedIndices</code>).</li> <li>Eager: <code>EnsureIndexes</code> / <code>EnsureIndexesAsync</code> scans an assembly for concrete <code>Schema&lt;TId&gt;</code> types, creates indexes up front, and throws <code>IndexVerificationException</code> if an expected index cannot be confirmed.</li> </ol>"},{"location":"breaking-changes/","title":"Breaking Changes &amp; Release Notes","text":"<p>This page details major release changes and migration guidance across Meerkat versions.</p>"},{"location":"breaking-changes/#v300","title":"v3.0.0","text":""},{"location":"breaking-changes/#key-enhancements-changes","title":"Key Enhancements &amp; Changes","text":"<ul> <li>Ambient Multi-Document Transactions:   Introduced <code>Meerkat.WithTransaction</code> and <code>Meerkat.WithTransactionAsync</code>. Reads and writes inside transaction callbacks automatically participate in the active session without requiring manual session parameters.</li> <li>Index Verification at Startup:   Added <code>Meerkat.EnsureIndexes</code> and <code>Meerkat.EnsureIndexesAsync</code> to idempotently create and verify attribute indexes upfront. Throws <code>IndexVerificationException</code> if an expected index cannot be confirmed.</li> <li>TTL Indexes:   Added <code>ExpireAfter</code> on <code>[SingleFieldIndex]</code> supporting human-readable duration strings (e.g., <code>\"30d\"</code>, <code>\"12h\"</code>, <code>\"15m\"</code>) and ISO-8601 durations on <code>DateTime</code>/<code>DateTime?</code> properties.</li> <li>Compound Unique Indexes:   Added <code>Unique</code> property to <code>[CompoundIndex]</code> to enforce compound uniqueness across grouped fields.</li> <li>Soft Delete Support:   Added opt-in soft delete via <code>[Collection(SoftDelete = true)]</code>. Automatically excludes soft-deleted records from queries, counts, and updates; provides <code>Restore*</code> and <code>HardRemove*</code> for restoring and permanently deleting documents.</li> <li>Fluent Atomic Partial Updates:   Added fluent <code>Update</code>, <code>UpdateOne</code>, <code>UpdateMany</code>, and <code>UpdateByFilter</code> builders supporting <code>$set</code>, <code>$unset</code>, <code>$push</code>, <code>$pull</code>, <code>$addToSet</code>, and <code>$inc</code>.</li> <li>Dependency Upgrades:   Upgraded <code>MongoDB.Driver</code> to 3.12.0 and replaced <code>mongo-url-parser</code> with <code>ciu-parser</code>.</li> </ul>"},{"location":"breaking-changes/#v200","title":"v2.0.0","text":""},{"location":"breaking-changes/#breaking-changes","title":"Breaking Changes","text":"<ul> <li>MongoDB Driver 3.x Upgrade:   The underlying <code>MongoDB.Driver</code> was upgraded to 3.x.</li> <li>Generic <code>Schema&lt;TId&gt;</code> Base Class:   The non-generic <code>Schema</code> base class was replaced by the generic <code>Schema&lt;TId&gt;</code>, requiring an explicit ID type parameter on all models and static <code>Meerkat</code> methods (e.g., <code>Meerkat.FindByIdAsync&lt;Student, ObjectId&gt;(id)</code>).</li> </ul>"},{"location":"indexing/","title":"Indexing","text":"<p>Apply index attributes directly to model properties to configure MongoDB indexes declaratively. Meerkat creates these indexes automatically on first collection access, or explicitly at application startup.</p>"},{"location":"indexing/#single-field-indexes","title":"Single-Field Indexes","text":"<p>Apply <code>[SingleFieldIndex]</code> to index a single property:</p> <pre><code>public class User : Schema&lt;Guid&gt;\n{\n    [SingleFieldIndex(Name = \"username_idx\", Sparse = true, IndexOrder = IndexOrder.Ascending)]\n    public string Username { get; set; } = string.Empty;\n\n    [SingleFieldIndex(ExpireAfter = \"30d\")]\n    public DateTime CreatedAtUtc { get; set; }\n}\n</code></pre>"},{"location":"indexing/#options","title":"Options","text":"Property Type Default Description <code>Name</code> <code>string</code> Auto Custom index name in MongoDB. <code>Sparse</code> <code>bool</code> <code>false</code> When <code>true</code>, excludes documents that do not contain the indexed field. <code>IndexOrder</code> <code>IndexOrder</code> <code>Ascending</code> Index sorting direction: <code>Ascending</code>, <code>Descending</code>, or <code>Hashed</code>. <code>ExpireAfter</code> <code>string</code> <code>null</code> TTL duration string (e.g. <code>\"30d\"</code>, <code>\"12h\"</code>, <code>\"15m\"</code>, <code>\"1s\"</code>, <code>\"500ms\"</code>, <code>\"2w\"</code>, or ISO-8601). Only valid on <code>DateTime</code> or <code>DateTime?</code> properties."},{"location":"indexing/#unique-indexes","title":"Unique Indexes","text":"<p>Apply <code>[UniqueIndex]</code> to enforce uniqueness on a single property:</p> <pre><code>public class User : Schema&lt;Guid&gt;\n{\n    [UniqueIndex(Name = \"email_idx\", Sparse = true)]\n    public string Email { get; set; } = string.Empty;\n}\n</code></pre>"},{"location":"indexing/#options_1","title":"Options","text":"Property Type Default Description <code>Name</code> <code>string</code> Auto Custom index name in MongoDB. <code>Sparse</code> <code>bool</code> <code>false</code> When <code>true</code>, allows multiple documents with missing or null values."},{"location":"indexing/#compound-indexes","title":"Compound Indexes","text":"<p>Properties sharing the same <code>Name</code> value are combined into a single compound index:</p> <pre><code>public class Order : Schema&lt;Guid&gt;\n{\n    [CompoundIndex(Name = \"order_idx\", IndexOrder = IndexOrder.Ascending, Unique = true)]\n    public DateTime OrderDate { get; set; }\n\n    [CompoundIndex(Name = \"order_idx\", IndexOrder = IndexOrder.Descending, Unique = true)]\n    public decimal TotalAmount { get; set; }\n}\n</code></pre>"},{"location":"indexing/#options_2","title":"Options","text":"Property Type Default Description <code>Name</code> <code>string</code> Auto Index name that groups fields together into a compound index. <code>IndexOrder</code> <code>IndexOrder</code> <code>Ascending</code> Direction: <code>Ascending</code>, <code>Descending</code>, or <code>Hashed</code>. <code>Unique</code> <code>bool</code> <code>false</code> When <code>true</code>, enforces compound uniqueness across all fields in the index. All members in the same named group must specify matching <code>Unique</code> settings. <p>Note</p> <p>Fields without an explicit <code>Name</code> are grouped together into a default unnamed compound index.</p>"},{"location":"indexing/#geospatial-indexes","title":"Geospatial Indexes","text":"<p>Apply <code>[GeospatialIndex]</code> for 2D or 2DSphere spatial queries:</p> <pre><code>public class Location : Schema&lt;Guid&gt;\n{\n    [GeospatialIndex(Name = \"coords_idx\", IndexType = GeospatialIndexType.TwoDSphere)]\n    public double[] Coordinates { get; set; } = Array.Empty&lt;double&gt;();\n}\n</code></pre>"},{"location":"indexing/#options_3","title":"Options","text":"Property Type Default Description <code>Name</code> <code>string</code> Auto Custom index name in MongoDB. <code>IndexType</code> <code>GeospatialIndexType</code> <code>TwoD</code> Index type: <code>TwoD</code> for flat geometry, or <code>TwoDSphere</code> for Earth-surface spherical coordinates."},{"location":"indexing/#index-summary","title":"Index Summary","text":"Attribute Scope Key Properties <code>[SingleFieldIndex]</code> Single property <code>Name</code>, <code>Sparse</code>, <code>IndexOrder</code>, <code>ExpireAfter</code> <code>[UniqueIndex]</code> Single property <code>Name</code>, <code>Sparse</code> <code>[CompoundIndex]</code> Multiple properties <code>Name</code> (grouping key), <code>IndexOrder</code>, <code>Unique</code> <code>[GeospatialIndex]</code> Single property <code>Name</code>, <code>IndexType</code>"},{"location":"indexing/#verifying-indexes-at-startup","title":"Verifying Indexes at Startup","text":"<p>While Meerkat automatically creates indexes when a collection is first accessed at runtime, types that are not accessed immediately will not have their indexes created until accessed.</p> <p>To guarantee that all indexes exist before serving application traffic, call <code>EnsureIndexes</code> or <code>EnsureIndexesAsync</code> right after <code>Connect</code>:</p> <pre><code>// Verify a single type\nawait Meerkat.EnsureIndexesAsync&lt;User, Guid&gt;();\n\n// Verify a set of types\nawait Meerkat.EnsureIndexesAsync(new[] { typeof(User), typeof(Order) });\n\n// Verify all Schema&lt;TId&gt; types in an assembly\nawait Meerkat.EnsureIndexesAsync(typeof(User).Assembly);\n\n// Synchronous variants\nMeerkat.EnsureIndexes&lt;User, Guid&gt;();\nMeerkat.EnsureIndexes(typeof(User).Assembly);\n</code></pre>"},{"location":"indexing/#verification-behaviour","title":"Verification Behaviour","text":"<p><code>EnsureIndexes</code> creates the expected indexes (idempotently) and checks the collection's indexes in MongoDB to verify that each declared index exists.</p> <p>If an expected index is missing or could not be created, Meerkat throws an <code>IndexVerificationException</code> containing details about the collection and missing indexes.</p>"},{"location":"models/","title":"Defining Models","text":"<p>All models in Meerkat must inherit from the abstract base class <code>Schema&lt;TId&gt;</code>, where <code>TId</code> is the type of the document's unique identifier. The <code>Id</code> property is mapped to the MongoDB <code>_id</code> field.</p>"},{"location":"models/#basic-model-definition","title":"Basic Model Definition","text":"<pre><code>using meerkat;\nusing MongoDB.Bson;\n\npublic class Student : Schema&lt;ObjectId&gt;\n{\n    public string FirstName { get; set; } = string.Empty;\n    public string LastName { get; set; } = string.Empty;\n\n    public Student()\n    {\n        Id = ObjectId.GenerateNewId();\n    }\n}\n</code></pre>"},{"location":"models/#identifier-types","title":"Identifier Types","text":"<p>The generic type parameter <code>TId</code> requires <code>IEquatable&lt;TId&gt;</code>. Common ID types supported in Meerkat include:</p> <ul> <li><code>MongoDB.Bson.ObjectId</code></li> <li><code>System.Guid</code></li> <li><code>string</code></li> <li><code>int</code> / <code>long</code></li> <li>Any custom value type implementing <code>IEquatable&lt;TId&gt;</code></li> </ul>"},{"location":"models/#collection-configuration","title":"Collection Configuration","text":"<p>Apply the <code>[Collection]</code> attribute to customise collection names, timestamp tracking, and soft deletion:</p> <pre><code>using meerkat;\nusing meerkat.Attributes;\nusing MongoDB.Bson;\n\n[Collection(Name = \"people\", TrackTimestamps = true, SoftDelete = true)]\npublic class Student : Schema&lt;ObjectId&gt;\n{\n    public string FirstName { get; set; } = string.Empty;\n    public string LastName { get; set; } = string.Empty;\n}\n</code></pre>"},{"location":"models/#attribute-options","title":"Attribute Options","text":"Property Type Default Description <code>Name</code> <code>string</code> <code>null</code> Overrides the default collection name. If not specified, Meerkat generates a pluralised, lowercased name from the class name (e.g. <code>Student</code> \u2192 <code>students</code>). <code>TrackTimestamps</code> <code>bool</code> <code>false</code> When <code>true</code>, automatically sets <code>CreatedAt</code> on first save and updates <code>UpdatedAt</code> on every save or partial update. <code>SoftDelete</code> <code>bool</code> <code>false</code> When <code>true</code>, enables soft delete. <code>Remove*</code> sets <code>DeletedAt</code> instead of physically deleting documents, and queries automatically exclude soft-deleted records."},{"location":"models/#timestamps","title":"Timestamps","text":"<p>When <code>TrackTimestamps = true</code> is set on the <code>[Collection]</code> attribute:</p> <ul> <li><code>CreatedAt</code> (<code>DateTime?</code>): Populated with UTC timestamp when the document is first saved via <code>Save</code> or <code>SaveAll</code>.</li> <li><code>UpdatedAt</code> (<code>DateTime?</code>): Updated to the current UTC timestamp on every <code>Save</code>, <code>SaveAll</code>, and fluent partial <code>Update</code> execution.</li> </ul> <pre><code>[Collection(TrackTimestamps = true)]\npublic class Article : Schema&lt;Guid&gt;\n{\n    public string Title { get; set; } = string.Empty;\n    public string Content { get; set; } = string.Empty;\n}\n</code></pre>"},{"location":"models/#soft-delete-overview","title":"Soft Delete Overview","text":"<p>When <code>SoftDelete = true</code> is set:</p> <ul> <li><code>DeletedAt</code> (<code>DateTimeOffset?</code>): Persisted as a UTC BSON date when the document is soft-deleted.</li> <li><code>IsDeleted</code> (<code>bool</code>): A computed property returning <code>DeletedAt != null</code> (ignored during serialisation).</li> <li>An index on <code>DeletedAt</code> (<code>deleted_at_idx</code>) is created automatically.</li> </ul> <p>For querying, restoring, and permanently deleting soft-deleted entities, see Persistence &amp; Transactions.</p>"},{"location":"persistence/","title":"Persistence &amp; Transactions","text":"<p>Meerkat provides rich APIs for single-document persistence, bulk persistence, physical and soft removal, and ambient multi-document transactions.</p>"},{"location":"persistence/#single-document-persistence","title":"Single Document Persistence","text":"<p><code>Save</code> and <code>SaveAsync</code> perform an upsert on the underlying collection: inserting the document if it does not exist, or replacing the entire document if a match on <code>Id</code> is found.</p> <pre><code>var student = new Student { FirstName = \"Ada\", LastName = \"Lovelace\" };\n\n// Asynchronous save\nawait student.SaveAsync();\n\n// Synchronous save\nstudent.Save();\n</code></pre> <p>When saving: 1. <code>CreatedAt</code> (if not already set) and <code>UpdatedAt</code> are populated if <code>TrackTimestamps = true</code>. 2. Property transformations (<code>[Lowercase]</code>, <code>[Uppercase]</code>) are executed. 3. <code>PreSave()</code> lifecycle hook is invoked. 4. The document is replaced / inserted in MongoDB using <code>ReplaceOne</code>. 5. <code>PostSave()</code> lifecycle hook is invoked.</p>"},{"location":"persistence/#bulk-persistence","title":"Bulk Persistence","text":"<p>To persist multiple entities in a single batched operation, use the <code>SaveAll</code> / <code>SaveAllAsync</code> extension methods from the <code>meerkat.Collections</code> namespace:</p> <pre><code>using meerkat.Collections;\n\nvar students = new[]\n{\n    new Student { FirstName = \"Ada\", LastName = \"Lovelace\" },\n    new Student { FirstName = \"Charles\", LastName = \"Babbage\" }\n};\n\n// Asynchronous bulk save\nawait students.SaveAllAsync&lt;Student, ObjectId&gt;();\n\n// Synchronous bulk save\nstudents.SaveAll&lt;Student, ObjectId&gt;();\n</code></pre>"},{"location":"persistence/#removal","title":"Removal","text":""},{"location":"persistence/#remove-by-id","title":"Remove by ID","text":"<pre><code>// Async\nawait Meerkat.RemoveByIdAsync&lt;Student, ObjectId&gt;(id);\n\n// Sync\nMeerkat.RemoveById&lt;Student, ObjectId&gt;(id);\n</code></pre>"},{"location":"persistence/#remove-first-match","title":"Remove First Match","text":"<pre><code>// Async\nawait Meerkat.RemoveOneAsync&lt;Student, ObjectId&gt;(x =&gt; x.FirstName == \"Ada\");\n\n// Sync\nMeerkat.RemoveOne&lt;Student, ObjectId&gt;(x =&gt; x.FirstName == \"Ada\");\n</code></pre>"},{"location":"persistence/#remove-all-matches","title":"Remove All Matches","text":"<pre><code>// Async\nawait Meerkat.RemoveAsync&lt;Student, ObjectId&gt;(x =&gt; x.LastName == \"Lovelace\");\n\n// Sync\nMeerkat.Remove&lt;Student, ObjectId&gt;(x =&gt; x.LastName == \"Lovelace\");\n</code></pre>"},{"location":"persistence/#model-instance-removal","title":"Model Instance Removal","text":"<p>Models inheriting from <code>Schema&lt;TId&gt;</code> can remove themselves directly:</p> <pre><code>await student.DeleteAsync();\n</code></pre>"},{"location":"persistence/#soft-delete","title":"Soft Delete","text":"<p>Opt into soft deletion per collection by setting <code>[Collection(SoftDelete = true)]</code>.</p> <p>Meerkat stores <code>DeletedAt</code> (<code>DateTimeOffset?</code>) as a UTC BSON date. The <code>IsDeleted</code> property is computed dynamically (<code>DeletedAt != null</code>) and is not persisted.</p> <pre><code>[Collection(SoftDelete = true, TrackTimestamps = true)]\npublic class Student : Schema&lt;ObjectId&gt;\n{\n    public string FirstName { get; set; } = string.Empty;\n}\n</code></pre>"},{"location":"persistence/#behaviour-when-enabled","title":"Behaviour When Enabled","text":"<ul> <li>Queries &amp; Counts: <code>Query</code>, <code>Find*</code>, <code>FindById*</code>, <code>Count*</code>, <code>Exists*</code>, increment/decrement, and fluent <code>Update*</code> automatically exclude soft-deleted documents.</li> <li>Deletions: <code>Remove*</code>, <code>RemoveById*</code>, <code>RemoveOne*</code>, and <code>student.DeleteAsync()</code> set <code>DeletedAt</code> (and <code>UpdatedAt</code> if timestamps are tracked) instead of issuing a physical MongoDB delete command.</li> <li>Index: An index on <code>DeletedAt</code> (<code>deleted_at_idx</code>) is created automatically.</li> </ul>"},{"location":"persistence/#including-soft-deleted-documents","title":"Including Soft-Deleted Documents","text":"<p>Pass <code>includeDeleted: true</code> to bypass the soft delete filter:</p> <pre><code>var active = await Meerkat.FindByIdAsync&lt;Student, ObjectId&gt;(id);\nvar includingDeleted = await Meerkat.FindByIdAsync&lt;Student, ObjectId&gt;(id, includeDeleted: true);\n\nvar all = await Meerkat.Query&lt;Student, ObjectId&gt;(includeDeleted: true).ToListAsync();\nlong totalCount = await Meerkat.CountAsync&lt;Student, ObjectId&gt;(includeDeleted: true);\n</code></pre>"},{"location":"persistence/#restoring-soft-deleted-documents","title":"Restoring Soft-Deleted Documents","text":"<pre><code>// Restore by ID\nawait Meerkat.RestoreByIdAsync&lt;Student, ObjectId&gt;(id);\n\n// Restore by predicate\nawait Meerkat.RestoreAsync&lt;Student, ObjectId&gt;(x =&gt; x.LastName == \"Lovelace\");\n\n// Instance helper\nawait student.RestoreAsync();\n</code></pre>"},{"location":"persistence/#hard-permanent-removal","title":"Hard (Permanent) Removal","text":"<p>To bypass soft delete and permanently delete documents from MongoDB:</p> <pre><code>// Hard remove by ID\nawait Meerkat.HardRemoveByIdAsync&lt;Student, ObjectId&gt;(id);\n\n// Hard remove by predicate\nawait Meerkat.HardRemoveAsync&lt;Student, ObjectId&gt;(x =&gt; x.LastName == \"Lovelace\");\n</code></pre> <p>Note</p> <p>Soft delete does not cascade to related documents (Meerkat does not maintain an association graph). Standard unique indexes still apply to soft-deleted records. If uniqueness is only required among active records, define a partial unique index.</p>"},{"location":"persistence/#transactions","title":"Transactions","text":"<p>Meerkat exposes ambient multi-document transactions. Writes and reads executed within the callback automatically join the transaction without requiring explicit session parameters on <code>Save</code>, <code>Remove*</code>, <code>Update*</code>, <code>Increment*</code>, <code>Count*</code>, or <code>Query</code>/<code>Find*</code> APIs.</p> <p>Replica Set Requirement</p> <p>MongoDB multi-document transactions require a replica set or sharded cluster. Standalone MongoDB instances do not support transactions.</p> <pre><code>await Meerkat.WithTransactionAsync(async _ =&gt;\n{\n    var order = new Order { Number = \"1001\" };\n    await order.SaveAsync();\n\n    var line = new OrderLine { OrderId = order.Id, Qty = 2 };\n    await line.SaveAsync();\n});\n</code></pre>"},{"location":"persistence/#transaction-semantics","title":"Transaction Semantics","text":"<ul> <li>Commit: If the callback completes successfully, the transaction commits automatically.</li> <li>Rollback: If the callback throws an exception, the transaction rolls back all modifications.</li> <li>Read-Your-Writes: Reads inside the callback see uncommitted writes made earlier in the same transaction.</li> <li>Nesting: Nested <code>WithTransaction</code> / <code>WithTransactionAsync</code> calls throw <code>InvalidOperationException</code>.</li> </ul>"},{"location":"persistence/#overloads-and-return-values","title":"Overloads and Return Values","text":"<pre><code>// Synchronous transaction\nMeerkat.WithTransaction(() =&gt;\n{\n    var order = new Order { Number = \"1002\" };\n    order.Save();\n});\n\n// Returning a value from transaction\nvar orderId = await Meerkat.WithTransactionAsync(async _ =&gt;\n{\n    var order = new Order { Number = \"1003\" };\n    await order.SaveAsync();\n    return order.Id;\n});\n</code></pre>"},{"location":"querying/","title":"Querying","text":"<p>All static query methods on <code>Meerkat</code> require two generic type parameters: the schema type (<code>TSchema</code>) and the identifier type (<code>TId</code>).</p>"},{"location":"querying/#find-by-id","title":"Find by ID","text":"<p>Retrieve a single document by its identifier:</p> <pre><code>// Async\nvar student = await Meerkat.FindByIdAsync&lt;Student, ObjectId&gt;(id);\n\n// Sync\nvar student = Meerkat.FindById&lt;Student, ObjectId&gt;(id);\n</code></pre> <p>When soft delete is enabled, soft-deleted documents are excluded by default. Pass <code>includeDeleted: true</code> to include them:</p> <pre><code>var student = await Meerkat.FindByIdAsync&lt;Student, ObjectId&gt;(id, includeDeleted: true);\n</code></pre>"},{"location":"querying/#find-one-by-predicate","title":"Find One by Predicate","text":"<p>Find the first document matching a predicate expression:</p> <pre><code>// Async\nvar student = await Meerkat.FindOneAsync&lt;Student, ObjectId&gt;(x =&gt; x.FirstName == \"Ada\");\n\n// Sync\nvar student = Meerkat.FindOne&lt;Student, ObjectId&gt;(x =&gt; x.LastName == \"Lovelace\");\n</code></pre> <p>Omitting the predicate returns the first document found in the collection:</p> <pre><code>var firstStudent = await Meerkat.FindOneAsync&lt;Student, ObjectId&gt;();\n</code></pre>"},{"location":"querying/#find-many-by-predicate","title":"Find Many by Predicate","text":"<p>Retrieve all documents matching a predicate expression as a list:</p> <pre><code>// Async\nvar students = await Meerkat.FindAsync&lt;Student, ObjectId&gt;(x =&gt; x.LastName == \"Lovelace\");\n\n// Sync\nvar students = Meerkat.Find&lt;Student, ObjectId&gt;(x =&gt; x.LastName == \"Lovelace\");\n</code></pre> <p>Omitting the predicate returns all active documents in the collection:</p> <pre><code>var allStudents = await Meerkat.FindAsync&lt;Student, ObjectId&gt;();\n</code></pre>"},{"location":"querying/#linq-queries","title":"LINQ Queries","text":"<p>For complex filtering, projections, sorting, and pagination, use <code>Meerkat.Query&lt;TSchema, TId&gt;()</code> to obtain a MongoDB LINQ <code>IQueryable</code>:</p> <pre><code>using MongoDB.Driver.Linq;\n\nvar results = await Meerkat.Query&lt;Student, ObjectId&gt;()\n    .Where(x =&gt; x.FirstName.StartsWith(\"A\"))\n    .OrderBy(x =&gt; x.LastName)\n    .Skip(10)\n    .Take(20)\n    .ToListAsync();\n</code></pre> <p>To include soft-deleted documents in a LINQ query:</p> <pre><code>var allResults = await Meerkat.Query&lt;Student, ObjectId&gt;(includeDeleted: true)\n    .Where(x =&gt; x.Age &gt;= 18)\n    .ToListAsync();\n</code></pre>"},{"location":"querying/#counting","title":"Counting","text":"<p>Count total documents or documents matching a filter predicate:</p> <pre><code>// Total count\nlong total = await Meerkat.CountAsync&lt;Student, ObjectId&gt;();\n\n// Conditional count\nlong adultsCount = await Meerkat.CountAsync&lt;Student, ObjectId&gt;(x =&gt; x.Age &gt;= 18);\n\n// Including soft-deleted records\nlong allCount = await Meerkat.CountAsync&lt;Student, ObjectId&gt;(includeDeleted: true);\n\n// Sync variants\nlong totalSync = Meerkat.Count&lt;Student, ObjectId&gt;();\nlong matchingSync = Meerkat.Count&lt;Student, ObjectId&gt;(x =&gt; x.FirstName.StartsWith(\"A\"));\n</code></pre>"},{"location":"querying/#existence-checks","title":"Existence Checks","text":"<p>Check whether any documents exist or if at least one document matches a predicate:</p> <pre><code>// Any documents exist\nbool hasAny = await Meerkat.ExistsAsync&lt;Student, ObjectId&gt;();\n\n// Document matching predicate exists\nbool exists = await Meerkat.ExistsAsync&lt;Student, ObjectId&gt;(x =&gt; x.FirstName == \"Ada\");\n\n// Sync variants\nbool hasAnySync = Meerkat.Exists&lt;Student, ObjectId&gt;();\nbool existsSync = Meerkat.Exists&lt;Student, ObjectId&gt;(x =&gt; x.FirstName == \"Ada\");\n</code></pre>"},{"location":"transforms-hooks/","title":"Transforms &amp; Hooks","text":"<p>Meerkat provides built-in attributes for automatic string transformations and lifecycle methods for custom business logic before and after document persistence.</p>"},{"location":"transforms-hooks/#data-transformations","title":"Data Transformations","text":"<p>Apply <code>[Lowercase]</code> or <code>[Uppercase]</code> attributes to model string properties to transform their values automatically before each document save.</p> <pre><code>using meerkat;\nusing meerkat.Attributes;\n\npublic class User : Schema&lt;Guid&gt;\n{\n    [Lowercase]\n    public string Email { get; set; } = string.Empty;\n\n    [Uppercase]\n    public string CountryCode { get; set; } = string.Empty;\n}\n</code></pre>"},{"location":"transforms-hooks/#constraints-validation","title":"Constraints &amp; Validation","text":"<ul> <li>Both attributes are strictly restricted to properties of type <code>string</code>.</li> <li>Applying <code>[Lowercase]</code> or <code>[Uppercase]</code> to non-string properties throws an <code>InvalidAttributeException</code> at runtime when inspected.</li> <li>Null string values are safely ignored and remain <code>null</code>.</li> </ul>"},{"location":"transforms-hooks/#lifecycle-hooks","title":"Lifecycle Hooks","text":"<p>Override the <code>PreSave</code> and <code>PostSave</code> virtual methods on any model inheriting from <code>Schema&lt;TId&gt;</code> to run custom logic during document persistence:</p> <pre><code>using meerkat;\nusing MongoDB.Bson;\n\npublic class Student : Schema&lt;ObjectId&gt;\n{\n    public string FirstName { get; set; } = string.Empty;\n    public string LastName { get; set; } = string.Empty;\n\n    public override void PreSave()\n    {\n        // Executes after timestamp generation and case transformations,\n        // right before the document is written to MongoDB.\n        FirstName = FirstName?.Trim() ?? string.Empty;\n        LastName = LastName?.Trim() ?? string.Empty;\n    }\n\n    public override void PostSave()\n    {\n        // Executes immediately after the MongoDB ReplaceOne write completes.\n    }\n}\n</code></pre>"},{"location":"transforms-hooks/#execution-order-during-save","title":"Execution Order during Save","text":"<p>When calling <code>Save()</code> or <code>SaveAsync()</code> (and in bulk <code>SaveAll()</code> / <code>SaveAllAsync()</code>):</p> <ol> <li>Timestamps: Sets <code>CreatedAt</code> (if first save) and <code>UpdatedAt</code> (if <code>TrackTimestamps = true</code>).</li> <li>Transforms: Evaluates <code>[Lowercase]</code> and <code>[Uppercase]</code> attributes on string properties.</li> <li>PreSave: Calls the model's <code>PreSave()</code> method.</li> <li>Database Write: Executes <code>ReplaceOne</code> with upsert enabled on MongoDB.</li> <li>PostSave: Calls the model's <code>PostSave()</code> method.</li> </ol> <p>Write-Path Difference</p> <p>Partial updates (via <code>Update&lt;TSchema, TId&gt;()</code>, <code>UpdateOne</code>, <code>UpdateMany</code>, <code>UpdateByFilter</code>, or atomic increment/decrement helpers) do not invoke <code>PreSave()</code>, <code>PostSave()</code>, or data transformations because documents are modified directly at the field level within MongoDB.</p>"},{"location":"updates/","title":"Atomic Updates","text":"<p>While <code>Save</code> and <code>SaveAsync</code> replace the entire document, Meerkat provides fluent update builders and dedicated atomic numeric helpers for field-level modifications. These issue MongoDB update operators (<code>$set</code>, <code>$unset</code>, <code>$push</code>, <code>$pull</code>, <code>$addToSet</code>, <code>$inc</code>) directly without replacing the entire document.</p>"},{"location":"updates/#behaviour-bypassed-features","title":"Behaviour &amp; Bypassed Features","text":"<p>When executing partial updates or atomic increment/decrement operations:</p> <ul> <li>Timestamps: If <code>TrackTimestamps = true</code> is enabled on the model's <code>[Collection]</code> attribute, <code>UpdatedAt</code> is automatically set to UTC now as part of the update.</li> <li>Skipped Operations: Field-level updates bypass <code>CreatedAt</code>, <code>[Lowercase]</code>/<code>[Uppercase]</code> case transformations, and <code>PreSave()</code> / <code>PostSave()</code> lifecycle hooks because the complete entity is not materialised or fully rewritten.</li> </ul>"},{"location":"updates/#fluent-partial-updates","title":"Fluent Partial Updates","text":"<p>Use the fluent update builder to compose multiple update operators in a single atomic database operation:</p> <pre><code>// Update by ID\nawait Meerkat.Update&lt;Student, ObjectId&gt;(id)\n    .Set(x =&gt; x.LastName, \"Lovelace\")\n    .Unset(x =&gt; x.Nickname)\n    .Push(x =&gt; x.Tags, \"honour\")\n    .Pull(x =&gt; x.Tags, \"draft\")\n    .AddToSet(x =&gt; x.Tags, \"alumni\")\n    .Inc(x =&gt; x.Age, 1)\n    .ExecuteAsync();\n\n// Update first document matching predicate\nawait Meerkat.UpdateOne&lt;Student, ObjectId&gt;(x =&gt; x.FirstName == \"Ada\")\n    .Set(x =&gt; x.LastName, \"Lovelace\")\n    .ExecuteAsync();\n\n// Update all documents matching predicate\nawait Meerkat.UpdateMany&lt;Student, ObjectId&gt;(x =&gt; x.Age &lt; 18)\n    .Set(x =&gt; x.Status, \"minor\")\n    .ExecuteAsync();\n\n// Update using raw MongoDB filter definition\nvar filter = Builders&lt;Student&gt;.Filter.Eq(x =&gt; x.Status, \"pending\");\nawait Meerkat.UpdateByFilter&lt;Student, ObjectId&gt;(filter)\n    .Set(x =&gt; x.Status, \"active\")\n    .ExecuteAsync();\n</code></pre>"},{"location":"updates/#execution-methods","title":"Execution Methods","text":"<ul> <li><code>ExecuteAsync()</code> / <code>Execute()</code>: Executes the update and returns the standard MongoDB <code>UpdateResult</code>.</li> <li><code>ExecuteAndGetUpdatedAsync()</code> / <code>ExecuteAndGetUpdated()</code>: Executes the update and returns the modified document after changes have been applied (cannot be used with <code>UpdateMany</code>).</li> </ul> <pre><code>var updatedStudent = await Meerkat.Update&lt;Student, ObjectId&gt;(id)\n    .Set(x =&gt; x.LastName, \"Lovelace\")\n    .ExecuteAndGetUpdatedAsync();\n</code></pre>"},{"location":"updates/#atomic-increment-decrement","title":"Atomic Increment &amp; Decrement","text":"<p>Meerkat provides dedicated atomic helpers for incrementing and decrementing numeric fields (<code>int</code>, <code>long</code>, <code>double</code>, <code>decimal</code>, etc.). If the amount is omitted, it defaults to <code>1</code>.</p>"},{"location":"updates/#increment-decrement-by-id","title":"Increment / Decrement by ID","text":"<pre><code>// Increment\nawait Meerkat.IncrementByIdAsync&lt;Student, ObjectId, int&gt;(id, x =&gt; x.Age, 5);\nMeerkat.IncrementById&lt;Student, ObjectId, int&gt;(id, x =&gt; x.Age); // default +1\n\n// Decrement\nawait Meerkat.DecrementByIdAsync&lt;Student, ObjectId, int&gt;(id, x =&gt; x.Age, 3);\nMeerkat.DecrementById&lt;Student, ObjectId, int&gt;(id, x =&gt; x.Age); // default -1\n</code></pre>"},{"location":"updates/#increment-decrement-by-filter","title":"Increment / Decrement by Filter","text":"<pre><code>var filter = Builders&lt;Student&gt;.Filter.Eq(x =&gt; x.FirstName, \"Ada\");\n\nawait Meerkat.IncrementByFilterAsync&lt;Student, ObjectId, int&gt;(filter, x =&gt; x.Age, 5);\nawait Meerkat.DecrementByFilterAsync&lt;Student, ObjectId, int&gt;(filter, x =&gt; x.Age, 3);\n</code></pre>"},{"location":"updates/#increment-decrement-one-by-predicate","title":"Increment / Decrement One by Predicate","text":"<pre><code>await Meerkat.IncrementOneAsync&lt;Student, ObjectId, int&gt;(x =&gt; x.FirstName == \"Ada\", x =&gt; x.Age, 5);\nawait Meerkat.DecrementOneAsync&lt;Student, ObjectId, int&gt;(x =&gt; x.FirstName == \"Ada\", x =&gt; x.Age, 3);\n</code></pre>"},{"location":"updates/#increment-decrement-many-by-predicate","title":"Increment / Decrement Many by Predicate","text":"<pre><code>await Meerkat.IncrementManyAsync&lt;Student, ObjectId, int&gt;(x =&gt; x.Age &lt; 18, x =&gt; x.Age, 1);\nawait Meerkat.DecrementManyAsync&lt;Student, ObjectId, int&gt;(x =&gt; x.Age &gt;= 65, x =&gt; x.Age, 1);\n</code></pre>"},{"location":"updates/#returning-the-updated-document","title":"Returning the Updated Document","text":"<p>Use <code>*AndGetUpdated</code> variants to receive the document after the numeric update:</p> <pre><code>var updated = await Meerkat.IncrementByIdAndGetUpdatedAsync&lt;Student, ObjectId, int&gt;(id, x =&gt; x.Age, 5);\nvar updated = await Meerkat.DecrementOneAndGetUpdatedAsync&lt;Student, ObjectId, int&gt;(x =&gt; x.FirstName == \"Ada\", x =&gt; x.Age, 3);\n</code></pre>"}]}